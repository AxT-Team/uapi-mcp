/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { imagePostImageOcr } from "../../funcs/imagePostImageOcr.js";
import { PostImageOcrRequest$zodSchema } from "../../models/postimageocrop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PostImageOcrRequest$zodSchema.describe(
    `包含待识别图像及可选配置的表单数据。无论使用哪种输入方式，请求体均需采用 \`multipart/form-data\` 格式。请在 \`file\`、\`url\` 或 \`image_base64\` 中选择一种作为输入源。`,
  ),
};

export const tool$imagePostImageOcr: ToolDefinition<typeof args> = {
  name: "image-post-image-ocr",
  description: `通用 OCR 文字识别

无论您是需要实现票据的自动化录入，还是在网页前端对图片上的文字进行坐标框选，这个高精度的 OCR 接口都能为您提供强大的基础能力。

## 功能概述
> [!IMPORTANT]
> 如果您只关心图片上写了什么（例如截图取字或内容安全审核），强烈建议将 \`need_location\` 设置为 \`false\`。这会大幅精简返回的 JSON 数据体积，提升网络传输与系统解析效率。

除了常规的图片转文字，这个接口还针对实际开发场景做了一些实用设计：
- **前端文字高亮与结构化分析**：默认返回每一段文字的矩形坐标和四个顶点坐标。这非常适合使用 Canvas 在原图上画框高亮，或者在后端根据相对位置提取票据中的键值对信息。
- **复杂拍摄环境下的抗畸变**：针对手机拍摄导致的旋转或倾斜，可以开启 \`enable_cls=true\`。服务端在识别前会自动进行方向预校正，显著提升识别准确率。
- **灵活的输入与请求要求**：接口支持 \`file\`、\`url\` 或 \`image_base64\` 三种方式输入。请确保请求格式为 \`multipart/form-data\`，且图片链接在公网可直接访问。`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": true,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await imagePostImageOcr(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
