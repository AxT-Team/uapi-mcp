/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { imageGetImageQrcode } from "../../funcs/imageGetImageQrcode.js";
import { GetImageQrcodeRequest$zodSchema } from "../../models/getimageqrcodeop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetImageQrcodeRequest$zodSchema,
};

export const tool$imageGetImageQrcode: ToolDefinition<typeof args> = {
  name: "image-get-image-qrcode",
  description: `生成二维码

无论是网址、文本还是联系方式，通通可以变成一个二维码！这是一个非常灵活的二维码生成工具。

## 功能概述
你提供一段文本内容，我们为你生成对应的二维码图片。你可以自定义尺寸、前景色、背景色，还支持透明背景，并选择不同的返回格式以适应不同场景。`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": true,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await imageGetImageQrcode(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
