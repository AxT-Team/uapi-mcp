/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { postSearchAggregate } from "../../funcs/postSearchAggregate.js";
import { PostSearchAggregateRequest$zodSchema } from "../../models/postsearchaggregateop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PostSearchAggregateRequest$zodSchema,
};

export const tool$postSearchAggregate: ToolDefinition<typeof args> = {
  name: "-post-search-aggregate",
  description: `智能搜索

想在你的应用中集成搜索功能？这个接口可以帮你轻松实现实时网页搜索。

## 功能概述

UAPI Pro Search 可以根据查询内容返回更相关的搜索结果。你可以用它搜索任何关键词，也可以限定在特定网站或特定文件类型中搜索。

- **实时网页搜索**: 毫秒级响应，快速返回搜索结果
- **智能排序**: 根据查询内容返回更相关的结果
- **时间排序**: 支持按发布时间排序，获取最新内容
- **时间范围过滤**: 支持按天/周/月/年过滤结果
- **站内搜索**: 支持 \`site:\` 操作符，在指定网站内搜索
- **文件类型过滤**: 支持 \`filetype:\` 操作符，快速找到 PDF、Word 等特定格式文件
      `,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": true,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await postSearchAggregate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
