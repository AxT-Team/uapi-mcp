/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { getSearchEngines } from "../../funcs/getSearchEngines.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$getSearchEngines: ToolDefinition = {
  name: "-get-search-engines",
  description: `搜索引擎配置

获取搜索功能的详细信息，包括支持的能力、参数限制和使用说明。

## 功能概述

此接口返回搜索功能的完整配置信息，你可以用它来：
- 了解当前可用的搜索能力（如站内搜索、文件类型过滤等）
- 获取参数的默认值和限制范围
- 查看当前配置版本和可用状态

适合在应用初始化时调用，或用于动态配置搜索界面。
      `,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": true,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result] = await getSearchEngines(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
