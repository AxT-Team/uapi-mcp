/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { networkGetNetworkIpinfo } from "../../funcs/networkGetNetworkIpinfo.js";
import { GetNetworkIpinfoRequest$zodSchema } from "../../models/getnetworkipinfoop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetNetworkIpinfoRequest$zodSchema,
};

export const tool$networkGetNetworkIpinfo: ToolDefinition<typeof args> = {
  name: "network-get-network-ipinfo",
  description: `查询 IP

想知道一个IP地址或域名来自哪里？这个接口可以帮你定位它。默认返回标准结果；如果传 \`source=commercial\`，可以返回更完整的位置信息。

## 功能概述
提供一个公网IPv4、IPv6地址或域名，我们会查询并返回它的地理位置（国家、省份、城市）、经纬度、以及所属的运营商（ISP）和自治系统（ASN）信息。这在网络安全分析、访问来源统计等领域非常有用。

当传 \`source=commercial\` 时，响应中会补充更完整的市、区、运营商、时区、海拔等信息，响应时间可能会稍长。`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": true,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await networkGetNetworkIpinfo(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
