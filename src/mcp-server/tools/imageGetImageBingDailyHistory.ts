/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { imageGetImageBingDailyHistory } from "../../funcs/imageGetImageBingDailyHistory.js";
import { GetImageBingDailyHistoryRequest$zodSchema } from "../../models/getimagebingdailyhistoryop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetImageBingDailyHistoryRequest$zodSchema.optional(),
};

export const tool$imageGetImageBingDailyHistory: ToolDefinition<typeof args> = {
  name: "image-get-image-bing-daily-history",
  description: `查询必应壁纸历史

这个接口用于查询必应壁纸历史列表，也可以按日期精确查询某一天。默认按时间倒序返回 JSON。

## 功能概述
- 可以传 \`date\` 精确查询某一天，命中后只返回 1 条数据
- 不传 \`date\` 时，按时间倒序分页返回历史列表
- 可以传 \`resolution\` 让 \`image_url\` 直接对应 \`4k\` 或 \`1080\`
- 可以传 \`page\` 和 \`page_size\` 控制分页
- 每条记录都是扁平 JSON 对象，里面会包含标题、副标题、说明文案、版权信息、问答信息和图片地址等字段

## 参数说明
\`resolution\` 默认是 \`4k\`。
\`page\` 默认是 \`1\`，\`page_size\` 默认是 \`30\`，最大是 \`100\`。
当传了 \`date\` 以后，\`page\` 和 \`page_size\` 不生效。`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": true,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await imageGetImageBingDailyHistory(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
