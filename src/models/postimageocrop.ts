/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import * as b64$ from "../lib/base64.js";
import { ClosedEnum } from "../types/enums.js";

export type PostImageOcrFile3 = {
  fileName: string;
  content: Uint8Array | string;
};

export const PostImageOcrFile3$zodSchema: z.ZodType<PostImageOcrFile3> = z
  .object({
    content: z.string().describe("Base64-encoded binary content").transform(
      b64$.bytesFromBase64,
    ),
    fileName: z.string(),
  });

/**
 * 是否返回文字坐标信息。请传 `true` 或 `false`，不传时默认是 `true`。
 */
export const NeedLocation3 = {
  True: "true",
  False: "false",
} as const;
/**
 * 是否返回文字坐标信息。请传 `true` 或 `false`，不传时默认是 `true`。
 */
export type NeedLocation3 = ClosedEnum<typeof NeedLocation3>;

export const NeedLocation3$zodSchema = z.enum([
  "true",
  "false",
]).describe(
  "是否返回文字坐标信息。请传 `true` 或 `false`，不传时默认是 `true`。",
);

/**
 * 是否额外返回整理后的 Markdown 文本。请传 `true` 或 `false`，不传时默认是 `false`。
 */
export const ReturnMarkdown3 = {
  True: "true",
  False: "false",
} as const;
/**
 * 是否额外返回整理后的 Markdown 文本。请传 `true` 或 `false`，不传时默认是 `false`。
 */
export type ReturnMarkdown3 = ClosedEnum<typeof ReturnMarkdown3>;

export const ReturnMarkdown3$zodSchema = z.enum([
  "true",
  "false",
]).describe(
  "是否额外返回整理后的 Markdown 文本。请传 `true` 或 `false`，不传时默认是 `false`。",
);

/**
 * 是否开启额外的文字方向校正。请传 `true` 或 `false`，不传时默认是 `false`。
 */
export const EnableCls3 = {
  True: "true",
  False: "false",
} as const;
/**
 * 是否开启额外的文字方向校正。请传 `true` 或 `false`，不传时默认是 `false`。
 */
export type EnableCls3 = ClosedEnum<typeof EnableCls3>;

export const EnableCls3$zodSchema = z.enum([
  "true",
  "false",
]).describe(
  "是否开启额外的文字方向校正。请传 `true` 或 `false`，不传时默认是 `false`。",
);

export type PostImageOcrRequestBody3 = {
  file?: PostImageOcrFile3 | Blob | undefined;
  url?: string | undefined;
  image_base64: string;
  image_name?: string | undefined;
  need_location?: NeedLocation3 | undefined;
  return_markdown?: ReturnMarkdown3 | undefined;
  enable_cls?: EnableCls3 | undefined;
};

export const PostImageOcrRequestBody3$zodSchema: z.ZodType<
  PostImageOcrRequestBody3
> = z.object({
  enable_cls: EnableCls3$zodSchema.default("false").describe(
    "是否开启额外的文字方向校正。请传 `true` 或 `false`，不传时默认是 `false`。",
  ),
  file: z.lazy(() => PostImageOcrFile3$zodSchema).optional().describe(
    "待识别的图片文件。支持 JPG、JPEG、PNG、BMP、GIF、WebP 等常见格式，最大不超过 10MB。请勿与 url 或 image_base64 同时提交。",
  ),
  image_base64: z.string().describe(
    "图片的 Base64 字符串。可以传完整 Data URI，也可以只传纯 Base64 内容。请勿与 file 或 url 同时提交。",
  ),
  image_name: z.string().optional().describe(
    "自定义图片文件名。传链接或纯 Base64 时建议一起传，便于保留或推断扩展名。",
  ),
  need_location: NeedLocation3$zodSchema.default("true").describe(
    "是否返回文字坐标信息。请传 `true` 或 `false`，不传时默认是 `true`。",
  ),
  return_markdown: ReturnMarkdown3$zodSchema.default("false").describe(
    "是否额外返回整理后的 Markdown 文本。请传 `true` 或 `false`，不传时默认是 `false`。",
  ),
  url: z.string().optional().describe(
    "公网可直接访问的图片地址。请勿与 file 或 image_base64 同时提交。",
  ),
});

export type PostImageOcrFile2 = {
  fileName: string;
  content: Uint8Array | string;
};

export const PostImageOcrFile2$zodSchema: z.ZodType<PostImageOcrFile2> = z
  .object({
    content: z.string().describe("Base64-encoded binary content").transform(
      b64$.bytesFromBase64,
    ),
    fileName: z.string(),
  });

/**
 * 是否返回文字坐标信息。请传 `true` 或 `false`，不传时默认是 `true`。
 */
export const NeedLocation2 = {
  True: "true",
  False: "false",
} as const;
/**
 * 是否返回文字坐标信息。请传 `true` 或 `false`，不传时默认是 `true`。
 */
export type NeedLocation2 = ClosedEnum<typeof NeedLocation2>;

export const NeedLocation2$zodSchema = z.enum([
  "true",
  "false",
]).describe(
  "是否返回文字坐标信息。请传 `true` 或 `false`，不传时默认是 `true`。",
);

/**
 * 是否额外返回整理后的 Markdown 文本。请传 `true` 或 `false`，不传时默认是 `false`。
 */
export const ReturnMarkdown2 = {
  True: "true",
  False: "false",
} as const;
/**
 * 是否额外返回整理后的 Markdown 文本。请传 `true` 或 `false`，不传时默认是 `false`。
 */
export type ReturnMarkdown2 = ClosedEnum<typeof ReturnMarkdown2>;

export const ReturnMarkdown2$zodSchema = z.enum([
  "true",
  "false",
]).describe(
  "是否额外返回整理后的 Markdown 文本。请传 `true` 或 `false`，不传时默认是 `false`。",
);

/**
 * 是否开启额外的文字方向校正。请传 `true` 或 `false`，不传时默认是 `false`。
 */
export const EnableCls2 = {
  True: "true",
  False: "false",
} as const;
/**
 * 是否开启额外的文字方向校正。请传 `true` 或 `false`，不传时默认是 `false`。
 */
export type EnableCls2 = ClosedEnum<typeof EnableCls2>;

export const EnableCls2$zodSchema = z.enum([
  "true",
  "false",
]).describe(
  "是否开启额外的文字方向校正。请传 `true` 或 `false`，不传时默认是 `false`。",
);

export type PostImageOcrRequestBody2 = {
  file?: PostImageOcrFile2 | Blob | undefined;
  url: string;
  image_base64?: string | undefined;
  image_name?: string | undefined;
  need_location?: NeedLocation2 | undefined;
  return_markdown?: ReturnMarkdown2 | undefined;
  enable_cls?: EnableCls2 | undefined;
};

export const PostImageOcrRequestBody2$zodSchema: z.ZodType<
  PostImageOcrRequestBody2
> = z.object({
  enable_cls: EnableCls2$zodSchema.default("false").describe(
    "是否开启额外的文字方向校正。请传 `true` 或 `false`，不传时默认是 `false`。",
  ),
  file: z.lazy(() => PostImageOcrFile2$zodSchema).optional().describe(
    "待识别的图片文件。支持 JPG、JPEG、PNG、BMP、GIF、WebP 等常见格式，最大不超过 10MB。请勿与 url 或 image_base64 同时提交。",
  ),
  image_base64: z.string().optional().describe(
    "图片的 Base64 字符串。可以传完整 Data URI，也可以只传纯 Base64 内容。请勿与 file 或 url 同时提交。",
  ),
  image_name: z.string().optional().describe(
    "自定义图片文件名。传链接或纯 Base64 时建议一起传，便于保留或推断扩展名。",
  ),
  need_location: NeedLocation2$zodSchema.default("true").describe(
    "是否返回文字坐标信息。请传 `true` 或 `false`，不传时默认是 `true`。",
  ),
  return_markdown: ReturnMarkdown2$zodSchema.default("false").describe(
    "是否额外返回整理后的 Markdown 文本。请传 `true` 或 `false`，不传时默认是 `false`。",
  ),
  url: z.string().describe(
    "公网可直接访问的图片地址。请勿与 file 或 image_base64 同时提交。",
  ),
});

export type PostImageOcrFile1 = {
  fileName: string;
  content: Uint8Array | string;
};

export const PostImageOcrFile1$zodSchema: z.ZodType<PostImageOcrFile1> = z
  .object({
    content: z.string().describe("Base64-encoded binary content").transform(
      b64$.bytesFromBase64,
    ),
    fileName: z.string(),
  });

/**
 * 是否返回文字坐标信息。请传 `true` 或 `false`，不传时默认是 `true`。
 */
export const NeedLocation1 = {
  True: "true",
  False: "false",
} as const;
/**
 * 是否返回文字坐标信息。请传 `true` 或 `false`，不传时默认是 `true`。
 */
export type NeedLocation1 = ClosedEnum<typeof NeedLocation1>;

export const NeedLocation1$zodSchema = z.enum([
  "true",
  "false",
]).describe(
  "是否返回文字坐标信息。请传 `true` 或 `false`，不传时默认是 `true`。",
);

/**
 * 是否额外返回整理后的 Markdown 文本。请传 `true` 或 `false`，不传时默认是 `false`。
 */
export const ReturnMarkdown1 = {
  True: "true",
  False: "false",
} as const;
/**
 * 是否额外返回整理后的 Markdown 文本。请传 `true` 或 `false`，不传时默认是 `false`。
 */
export type ReturnMarkdown1 = ClosedEnum<typeof ReturnMarkdown1>;

export const ReturnMarkdown1$zodSchema = z.enum([
  "true",
  "false",
]).describe(
  "是否额外返回整理后的 Markdown 文本。请传 `true` 或 `false`，不传时默认是 `false`。",
);

/**
 * 是否开启额外的文字方向校正。请传 `true` 或 `false`，不传时默认是 `false`。
 */
export const EnableCls1 = {
  True: "true",
  False: "false",
} as const;
/**
 * 是否开启额外的文字方向校正。请传 `true` 或 `false`，不传时默认是 `false`。
 */
export type EnableCls1 = ClosedEnum<typeof EnableCls1>;

export const EnableCls1$zodSchema = z.enum([
  "true",
  "false",
]).describe(
  "是否开启额外的文字方向校正。请传 `true` 或 `false`，不传时默认是 `false`。",
);

export type PostImageOcrRequestBody1 = {
  file: PostImageOcrFile1 | Blob;
  url?: string | undefined;
  image_base64?: string | undefined;
  image_name?: string | undefined;
  need_location?: NeedLocation1 | undefined;
  return_markdown?: ReturnMarkdown1 | undefined;
  enable_cls?: EnableCls1 | undefined;
};

export const PostImageOcrRequestBody1$zodSchema: z.ZodType<
  PostImageOcrRequestBody1
> = z.object({
  enable_cls: EnableCls1$zodSchema.default("false").describe(
    "是否开启额外的文字方向校正。请传 `true` 或 `false`，不传时默认是 `false`。",
  ),
  file: z.lazy(() => PostImageOcrFile1$zodSchema).describe(
    "待识别的图片文件。支持 JPG、JPEG、PNG、BMP、GIF、WebP 等常见格式，最大不超过 10MB。请勿与 url 或 image_base64 同时提交。",
  ),
  image_base64: z.string().optional().describe(
    "图片的 Base64 字符串。可以传完整 Data URI，也可以只传纯 Base64 内容。请勿与 file 或 url 同时提交。",
  ),
  image_name: z.string().optional().describe(
    "自定义图片文件名。传链接或纯 Base64 时建议一起传，便于保留或推断扩展名。",
  ),
  need_location: NeedLocation1$zodSchema.default("true").describe(
    "是否返回文字坐标信息。请传 `true` 或 `false`，不传时默认是 `true`。",
  ),
  return_markdown: ReturnMarkdown1$zodSchema.default("false").describe(
    "是否额外返回整理后的 Markdown 文本。请传 `true` 或 `false`，不传时默认是 `false`。",
  ),
  url: z.string().optional().describe(
    "公网可直接访问的图片地址。请勿与 file 或 image_base64 同时提交。",
  ),
});

/**
 * 包含待识别图像及可选配置的表单数据。无论使用哪种输入方式，请求体均需采用 `multipart/form-data` 格式。请在 `file`、`url` 或 `image_base64` 中选择一种作为输入源。
 */
export type PostImageOcrRequest =
  | PostImageOcrRequestBody1
  | PostImageOcrRequestBody2
  | PostImageOcrRequestBody3;

export const PostImageOcrRequest$zodSchema: z.ZodType<PostImageOcrRequest> = z
  .union([
    z.lazy(() => PostImageOcrRequestBody1$zodSchema),
    z.lazy(() => PostImageOcrRequestBody2$zodSchema),
    z.lazy(() => PostImageOcrRequestBody3$zodSchema),
  ]).describe(
    "包含待识别图像及可选配置的表单数据。无论使用哪种输入方式，请求体均需采用 `multipart/form-data` 格式。请在 `file`、`url` 或 `image_base64` 中选择一种作为输入源。",
  );

/**
 * 文字识别服务暂时不可用，请稍后再试。
 */
export type PostImageOcrServiceUnavailableResponseBody = {
  code?: string | undefined;
  message?: string | undefined;
};

export const PostImageOcrServiceUnavailableResponseBody$zodSchema: z.ZodType<
  PostImageOcrServiceUnavailableResponseBody
> = z.object({
  code: z.string().optional(),
  message: z.string().optional(),
}).describe("文字识别服务暂时不可用，请稍后再试。");

/**
 * 识别处理失败，请稍后重试。
 */
export type PostImageOcrBadGatewayResponseBody = {
  code?: string | undefined;
  message?: string | undefined;
};

export const PostImageOcrBadGatewayResponseBody$zodSchema: z.ZodType<
  PostImageOcrBadGatewayResponseBody
> = z.object({
  code: z.string().optional(),
  message: z.string().optional(),
}).describe("识别处理失败，请稍后重试。");

/**
 * 上传内容不是可识别的常见图片格式。
 */
export type PostImageOcrUnsupportedMediaTypeResponseBody = {
  code?: string | undefined;
  message?: string | undefined;
};

export const PostImageOcrUnsupportedMediaTypeResponseBody$zodSchema: z.ZodType<
  PostImageOcrUnsupportedMediaTypeResponseBody
> = z.object({
  code: z.string().optional(),
  message: z.string().optional(),
}).describe("上传内容不是可识别的常见图片格式。");

/**
 * 图片大小超过当前限制。
 */
export type PostImageOcrRequestEntityTooLargeResponseBody = {
  code?: string | undefined;
  message?: string | undefined;
};

export const PostImageOcrRequestEntityTooLargeResponseBody$zodSchema: z.ZodType<
  PostImageOcrRequestEntityTooLargeResponseBody
> = z.object({
  code: z.string().optional(),
  message: z.string().optional(),
}).describe("图片大小超过当前限制。");

export type Base64 = {
  code?: string | undefined;
  message?: string | undefined;
};

export const Base64$zodSchema: z.ZodType<Base64> = z.object({
  code: z.string().optional(),
  message: z.string().optional(),
});

export type PostImageOcr2 = {
  code?: string | undefined;
  message?: string | undefined;
};

export const PostImageOcr2$zodSchema: z.ZodType<PostImageOcr2> = z.object({
  code: z.string().optional(),
  message: z.string().optional(),
});

export type PostImageOcr1 = {
  code?: string | undefined;
  message?: string | undefined;
};

export const PostImageOcr1$zodSchema: z.ZodType<PostImageOcr1> = z.object({
  code: z.string().optional(),
  message: z.string().optional(),
});

/**
 * 请求参数不正确，比如没有传图片来源、提交了多重图片来源，或者布尔参数和 Base64 格式不合法。
 */
export type PostImageOcrResponseBodyUnion =
  | PostImageOcr1
  | PostImageOcr2
  | Base64;

export const PostImageOcrResponseBodyUnion$zodSchema: z.ZodType<
  PostImageOcrResponseBodyUnion
> = z.union([
  z.lazy(() => PostImageOcr1$zodSchema),
  z.lazy(() => PostImageOcr2$zodSchema),
  z.lazy(() => Base64$zodSchema),
]).describe(
  "请求参数不正确，比如没有传图片来源、提交了多重图片来源，或者布尔参数和 Base64 格式不合法。",
);

/**
 * 当前文字片段的矩形坐标。只有在 `need_location=true` 时才会返回。
 */
export type Location = {
  left?: number | undefined;
  top?: number | undefined;
  width?: number | undefined;
  height?: number | undefined;
};

export const Location$zodSchema: z.ZodType<Location> = z.object({
  height: z.number().optional(),
  left: z.number().optional(),
  top: z.number().optional(),
  width: z.number().optional(),
}).describe("当前文字片段的矩形坐标。只有在 `need_location=true` 时才会返回。");

export type VertexesLocation = {
  x?: number | undefined;
  y?: number | undefined;
};

export const VertexesLocation$zodSchema: z.ZodType<VertexesLocation> = z.object(
  {
    x: z.number().optional(),
    y: z.number().optional(),
  },
);

export type WordsResult = {
  words?: string | undefined;
  location?: Location | undefined;
  vertexes_location?: Array<VertexesLocation> | undefined;
  score?: number | undefined;
};

export const WordsResult$zodSchema: z.ZodType<WordsResult> = z.object({
  location: z.lazy(() => Location$zodSchema).optional().describe(
    "当前文字片段的矩形坐标。只有在 `need_location=true` 时才会返回。",
  ),
  score: z.number().optional().describe(
    "当前文字片段的置信度。部分结果会返回。",
  ),
  vertexes_location: z.array(z.lazy(() => VertexesLocation$zodSchema))
    .optional().describe(
      "当前文字片段的顶点坐标列表。只有在 `need_location=true` 时才会返回。",
    ),
  words: z.string().optional().describe("当前文字片段的识别结果。"),
});

/**
 * 耗时拆分信息，适合做性能统计或排查。
 */
export type Timing = {};

export const Timing$zodSchema: z.ZodType<Timing> = z.object({}).describe(
  "耗时拆分信息，适合做性能统计或排查。",
);

/**
 * 识别结果的统计摘要。
 */
export type PostImageOcrSummary = {};

export const PostImageOcrSummary$zodSchema: z.ZodType<PostImageOcrSummary> = z
  .object({}).describe("识别结果的统计摘要。");

/**
 * 图片本身的基础信息。
 */
export type Image = {};

export const Image$zodSchema: z.ZodType<Image> = z.object({}).describe(
  "图片本身的基础信息。",
);

export type Line = {};

export const Line$zodSchema: z.ZodType<Line> = z.object({});

export type Block = {};

export const Block$zodSchema: z.ZodType<Block> = z.object({});

export type PostImageOcrPage = {};

export const PostImageOcrPage$zodSchema: z.ZodType<PostImageOcrPage> = z.object(
  {},
);

/**
 * 补充识别结果对象，适合需要继续解析更多细节字段的场景。
 */
export type Raw = {};

export const Raw$zodSchema: z.ZodType<Raw> = z.object({}).describe(
  "补充识别结果对象，适合需要继续解析更多细节字段的场景。",
);

/**
 * 识别成功，返回统一的 OCR 结果对象。默认会带坐标信息；当 `need_location=false` 时，坐标相关字段会省略。
 */
export type PostImageOcrResponseBody = {
  text?: string | undefined;
  plain_text?: string | undefined;
  markdown?: string | undefined;
  words_result?: Array<WordsResult> | undefined;
  words_result_num?: number | undefined;
  need_location?: boolean | undefined;
  timing?: Timing | undefined;
  summary?: PostImageOcrSummary | undefined;
  image?: Image | undefined;
  lines?: Array<Line> | undefined;
  blocks?: Array<Block> | undefined;
  pages?: Array<PostImageOcrPage> | undefined;
  raw?: Raw | undefined;
};

export const PostImageOcrResponseBody$zodSchema: z.ZodType<
  PostImageOcrResponseBody
> = z.object({
  blocks: z.array(z.lazy(() => Block$zodSchema)).optional().describe(
    "按块组织的详细识别结果。",
  ),
  image: z.lazy(() => Image$zodSchema).optional().describe(
    "图片本身的基础信息。",
  ),
  lines: z.array(z.lazy(() => Line$zodSchema)).optional().describe(
    "按行组织的详细识别结果。",
  ),
  markdown: z.string().optional().describe(
    "根据图片中的标题、段落和表格整理出的 Markdown 文本。只有在 `return_markdown=true` 时才会返回。",
  ),
  need_location: z.boolean().optional().describe("本次响应是否包含坐标信息。"),
  pages: z.array(z.lazy(() => PostImageOcrPage$zodSchema)).optional().describe(
    "按页组织的详细识别结果。",
  ),
  plain_text: z.string().optional().describe(
    "纯文本结果，适合做搜索、索引或直接展示。",
  ),
  raw: z.lazy(() => Raw$zodSchema).optional().describe(
    "补充识别结果对象，适合需要继续解析更多细节字段的场景。",
  ),
  summary: z.lazy(() => PostImageOcrSummary$zodSchema).optional().describe(
    "识别结果的统计摘要。",
  ),
  text: z.string().optional().describe("按阅读顺序拼接后的识别文本。"),
  timing: z.lazy(() => Timing$zodSchema).optional().describe(
    "耗时拆分信息，适合做性能统计或排查。",
  ),
  words_result: z.array(z.lazy(() => WordsResult$zodSchema)).optional()
    .describe("逐段文字结果。适合做高亮、框选和逐项解析。"),
  words_result_num: z.int().optional().describe("识别出的文字片段数量。"),
}).describe(
  "识别成功，返回统一的 OCR 结果对象。默认会带坐标信息；当 `need_location=false` 时，坐标相关字段会省略。",
);

export type PostImageOcrResponse =
  | PostImageOcrResponseBody
  | PostImageOcr1
  | PostImageOcr2
  | Base64
  | PostImageOcrRequestEntityTooLargeResponseBody
  | PostImageOcrUnsupportedMediaTypeResponseBody
  | PostImageOcrBadGatewayResponseBody
  | PostImageOcrServiceUnavailableResponseBody;

export const PostImageOcrResponse$zodSchema: z.ZodType<PostImageOcrResponse> = z
  .union([
    z.lazy(() => PostImageOcrResponseBody$zodSchema),
    z.union([
      z.lazy(() => PostImageOcr1$zodSchema),
      z.lazy(() => PostImageOcr2$zodSchema),
      z.lazy(() => Base64$zodSchema),
    ]),
    z.lazy(() => PostImageOcrRequestEntityTooLargeResponseBody$zodSchema),
    z.lazy(() => PostImageOcrUnsupportedMediaTypeResponseBody$zodSchema),
    z.lazy(() => PostImageOcrBadGatewayResponseBody$zodSchema),
    z.lazy(() => PostImageOcrServiceUnavailableResponseBody$zodSchema),
  ]);
